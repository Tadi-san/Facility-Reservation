services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-eagle}
      POSTGRES_USER: ${POSTGRES_USER:-eagle}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-eaglepass}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  backend:
    build:
      context: ./app/backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-eagle}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-eagle}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-eaglepass}
      JWT_SECRET: ${JWT_SECRET:-this-is-a-local-dev-secret-change-me}
      DATA_ENCRYPTION_KEY: ${DATA_ENCRYPTION_KEY:-0123456789ABCDEF0123456789ABCDEF}
    ports:
      - "8080:8080"
    depends_on:
      - postgres

  frontend:
    image: node:20
    working_dir: /workspace
    volumes:
      - ./app/frontend:/workspace
    command: sh -c "npm install && npm run start -- --host 0.0.0.0 --port 4200"
    ports:
      - "4200:4200"
    depends_on:
      - backend

volumes:
  pgdata:
